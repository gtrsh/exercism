export const QUIZ_ITEMS_COUNT = 3

export const wrongAnswer = (wrong, correct) => console.log(`${wrong} is wrong answer ;(. Correct answer was ${correct}.`)
export const gameFail = (name) => console.log(`Let's try again, ${name}!`)
export const gameSuccess = (name) => console.log(`Congratulations, ${name}!`)
